# Week 2 — ท่อ + คะแนน

## Recap Week 1

```python
bird_speed += GRAVITY    # นกตก
bird_y += bird_speed     # ขยับตำแหน่ง
clock.tick(60)           # จำกัดความเร็วเกม

if event.key == pygame.K_SPACE:
    bird_speed = JUMP    # กระโดด
```

---

## เป้าหมายวันนี้

1. วาดท่อ (สี่เหลี่ยมสีเขียว)
2. ขยับท่อไปทางซ้าย
3. ผ่านท่อ → `score += 1`

---

## ผลลัพธ์ที่จะได้

```
🟡 →  🟩 |  🟩
         ช่องว่าง  ← นกต้องบินผ่านตรงนี้
      🟩 |  🟩

Score: 0 → 1 → 2 ...
```

---

## ไฟล์ในโฟลเดอร์นี้

| ไฟล์ | ใช้ทำอะไร |
|------|-----------|
| `00X_....md` | **สไลด์** — เปิดให้เด็กดู แล้วพิมพ์ตาม |
| `flappy.py` | **เฉลย** — ครูใช้เช็ก / เด็กเทียบตอนจบคาบ |

---

## เริ่มจาก

ใช้ `flappy.py` จาก Week 1 ของตัวเอง (หรือคัดลอกเฉลย Week 1 มาแก้ต่อ)
