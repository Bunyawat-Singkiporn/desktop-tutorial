# Homework: Data Types — ข้อ 1: ชนิดของจำนวนเต็ม

**Difficulty:** 🟢 Easy

---

## โจทย์

เซ็นเซอร์อุณหภูมิเก็บค่าจำนวนเต็มไว้ในตัวแปร `x`

กำหนด `x = 7` แล้วแสดงชนิดข้อมูลด้วย `type`

**Output:**
```
<class 'int'>
```

---

## 💡 Hint

ใช้ `print(type(x))` เพื่อดูชนิด

---

## Starter Code

```python
x = 7
# แสดงผลตรงนี้
```
