# Homework: Loop Safety — ข้อ 1: หยุดก่อนถึงสาม

**Difficulty:** 🟢 Easy

---

## โจทย์

วน `range(10)` แต่ `break` เมื่อ `i == 3` (พิมพ์แค่ 0 1 2)

**Output:**
```
0
1
2
```

---

## 💡 Hint

break ก่อนหรือหลัง print ให้ตรงโจทย์นี้: พิมพ์ก่อนเช็กหรือเช็กก่อนพิมพ์ — ตามเฉลยคือเช็กก่อนแล้วค่อยพิมพ์ ดังนั้นไม่พิมพ์ 3

---

## Starter Code

```python
for i in range(10):
    # เขียนโค้ดตรงนี้
```
