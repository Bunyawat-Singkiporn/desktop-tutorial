# Homework: elif — ข้อ 2: ไซส์เสื้อจากตัวเลข

**Difficulty:** 🟢 Easy

---

## โจทย์

รับขนาดตัวเลข: `>=100` → `L`, `>=50` → `M`, อื่นๆ → `S`

**ตัวอย่าง** Input `75` → `M`

---

## 💡 Hint

เรียงเงื่อนไขจากมากไปน้อย

---

## Starter Code

```python
n = int(input())
if n >= 100:
    # เขียนโค้ดตรงนี้
elif n >= 50:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
