# Homework: elif — ข้อ 7: ความเร็วรถ

**Difficulty:** 🟢 Easy

---

## โจทย์

รับความเร็ว: `>=120` Fast, `>=60` OK, อื่นๆ Slow

**ตัวอย่าง** Input `60` → `OK`

---

## 💡 Hint

elif

---

## Starter Code

```python
v = int(input())
if v >= 120:
    # เขียนโค้ดตรงนี้
elif v >= 60:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
