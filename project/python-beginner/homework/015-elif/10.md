# Homework: elif — ข้อ 10: ผลการเรียนภาค

**Difficulty:** 🟢 Easy

---

## โจทย์

รับคะแนน: `>=60` Pass, `>=40` Retake, อื่นๆ Fail

**ตัวอย่าง** Input `45` → `Retake`

---

## 💡 Hint

3 ทางด้วย elif

---

## Starter Code

```python
s = int(input())
if s >= 60:
    # เขียนโค้ดตรงนี้
elif s >= 40:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
