# Homework: elif — ข้อ 5: เส้นผ่านเกณฑ์

**Difficulty:** 🟢 Easy

---

## โจทย์

รับคะแนน: `>50` Pass, `==50` Border, อื่นๆ Fail

**ตัวอย่าง** Input `50` → `Border`

---

## 💡 Hint

ระวังลำดับเงื่อนไข

---

## Starter Code

```python
s = int(input())
if s > 50:
    # เขียนโค้ดตรงนี้
elif s == 50:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
