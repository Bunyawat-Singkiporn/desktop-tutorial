# Homework: Variables — ข้อ 4: อัปเดตหมายเลขคิว

**Difficulty:** 🟢 Easy

---

## โจทย์

หมายเลขคิวถูกตั้งเป็น 1 ก่อน แล้วเปลี่ยนเป็น 2 ภายหลัง

กำหนด `n = 1` จากนั้นเปลี่ยนเป็น `n = 2` แล้วแสดงค่าล่าสุด

**Output:**
```
2
```

---

## 💡 Hint

เมื่อกำหนดค่าใหม่ให้ชื่อเดิม ค่าเก่าจะถูกแทนที่

---

## Starter Code

```python
n = 1
n = 2
# แสดงผลตรงนี้
```
