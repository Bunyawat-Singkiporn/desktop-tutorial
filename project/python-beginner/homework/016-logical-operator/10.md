# Homework: Logical Operators — ข้อ 10: ดาวเด่น

**Difficulty:** 🟢 Easy

---

## โจทย์

รับคะแนน, โบนัส (`1`/`0`), เหรียญวิเศษ (`1`/`0`) — ได้ `Star` เมื่อคะแนน `>=80` และ (โบนัสหรือเหรียญเป็น 1)

ไม่งั้น `Normal`

---

## 💡 Hint

and รวมกับ or ในวงเล็บ

---

## Starter Code

```python
s = int(input())
b = int(input())
m = int(input())
if s >= 80 and (b == 1 or m == 1):
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
