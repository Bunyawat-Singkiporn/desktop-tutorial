# Homework: Logical Operators — ข้อ 7: ล็อกอินง่าย

**Difficulty:** 🟢 Easy

---

## โจทย์

รับ username ถูกไหม (`1`/`0`) และ password ถูกไหม (`1`/`0`) — ทั้งคู่ถูกถึงจะ `Login` ไม่งั้น `Deny`

---

## 💡 Hint

and

---

## Starter Code

```python
u = int(input())
p = int(input())
if u == 1 and p == 1:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
