# Homework: Logical Operators — ข้อ 2: มีศูนย์ปนไหม

**Difficulty:** 🟢 Easy

---

## โจทย์

รับจำนวนสองตัว ถ้ามีอย่างน้อยหนึ่งตัวเป็น 0 แสดง `Has zero` ไม่เช่นนั้น `None`

**ตัวอย่าง** Input `0` แล้ว `8` → `Has zero`

---

## 💡 Hint

ใช้ `or`

---

## Starter Code

```python
a = int(input())
b = int(input())
if a == 0 or b == 0:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
