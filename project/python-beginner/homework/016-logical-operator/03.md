# Homework: Logical Operators — ข้อ 3: ไม่ฝนตกก็ออกไป

**Difficulty:** 🟢 Easy

---

## โจทย์

รับค่า raining เป็น `1` หรือ `0` — ถ้าไม่ฝน (`not`) แสดง `Go out` ไม่เช่นนั้น `Stay`

**ตัวอย่าง** Input `0` → `Go out`

---

## 💡 Hint

`if not r:`

---

## Starter Code

```python
r = int(input())
if not r:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
