# Homework: if/else — ข้อ 9: ชมเชยคะแนน

**Difficulty:** 🟢 Easy

---

## โจทย์

รับคะแนน ถ้า `>= 80` แสดง `Great` ไม่เช่นนั้น `Keep going`

**ตัวอย่าง** Input `80` → `Great`

---

## 💡 Hint

if/else

---

## Starter Code

```python
s = int(input())
if s >= 80:
    # เขียนโค้ดตรงนี้
else:
    # เขียนโค้ดตรงนี้
```
