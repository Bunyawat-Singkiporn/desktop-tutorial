# -*- coding: utf-8 -*-
"""Rewrite homework โจทย์/Hint to be clear exercise-like copy; keep answer/*.py; scaffold starters."""
from __future__ import annotations

import importlib.util
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent
HW = ROOT / "homework"

# Import scaffold helper
spec = importlib.util.spec_from_file_location("fix_hw", ROOT / "_fix_homework_starters.py")
fix_mod = importlib.util.module_from_spec(spec)
assert spec.loader is not None
spec.loader.exec_module(fix_mod)


def md(topic: str, n: int, title: str, body: str, hint: str, starter: str) -> str:
    return f"""# Homework: {topic} — ข้อ {n}: {title}

**Difficulty:** 🟢 Easy

---

## โจทย์

{body.strip()}

---

## 💡 Hint

{hint.strip()}

---

## Starter Code

```python
{starter.rstrip()}
```
"""


# week -> list of 10 (title, body, hint) — bodies must match existing answers
PROMPTS: dict[str, list[tuple[str, str, str]]] = {}

PROMPTS["001-what-is-python"] = [
    ("ทักทาย Python", "เขียนโปรแกรมแสดงข้อความต้อนรับบนหน้าจอ\n\n**Output:**\n```\nHello, Python!\n```", "ใช้ `print(\"...\")` หนึ่งครั้ง"),
    ("แนะนำตัว", "แสดงประโยคแนะนำตัวตามนี้เป๊ะๆ\n\n**Output:**\n```\nMy name is Sam\n```", "ข้อความต้องอยู่ในเครื่องหมายคำพูด"),
    ("สามตัวอักษร", "แสดงตัวอักษร A, B, C **คนละบรรทัด** ตามลำดับ\n\n**Output:**\n```\nA\nB\nC\n```", "ใช้ `print()` สามครั้ง"),
    ("เส้นคั่นสามแถว", "วาดเส้นคั่นด้วยดาวสามบรรทัด บรรทัดละ `***`\n\n**Output:**\n```\n***\n***\n***\n```", "พิมพ์ข้อความเดิมซ้ำได้"),
    ("หัวข้อคะแนน", "แสดงหัวข้อสำหรับหน้าคะแนน\n\n**Output:**\n```\n=== SCORE ===\n```", "นับช่องว่างและ `=` ให้ตรง"),
    ("สัตว์สองตัว", "แสดงชื่อสัตว์สองชนิด คนละบรรทัด\n\n**Output:**\n```\nCat\nDog\n```", "สองครั้ง `print()`"),
    ("ตัวเลขในคำพูด", "แสดงเลขหนึ่งร้อยในรูปข้อความ (ยังไม่ต้องใช้ตัวแปร)\n\n**Output:**\n```\n100\n```", 'ใช้ `print("100")`'),
    ("ทักทายแล้วบ๊ายบาย", "แสดง `Hi` แล้วเว้นหนึ่งบรรทัดว่าง แล้วแสดง `Bye`\n\n**Output:**\n```\nHi\n\nBye\n```", 'บรรทัดว่างใช้ `print("")` ได้'),
    ("กรอบสี่เหลี่ยมเล็ก", "วาดกรอบด้วยดาวตามรูปแบบนี้\n\n**Output:**\n```\n****\n*  *\n****\n```", "นับช่องว่างตรงกลางให้ได้ 2 ช่อง"),
    ("สวัสดีตอนเช้า", "แสดงคำทักทายตอนเช้า\n\n**Output:**\n```\nGood morning!\n```", "อย่าลืมเครื่องหมาย `!`"),
]

PROMPTS["002-python-environment"] = [
    ("โค้ดที่รันได้", "เขียน `print` ให้ถูกต้อง (ตัวพิมพ์เล็ก + วงเล็บครบ) เพื่อแสดงว่าพร้อมทำงาน\n\n**Output:**\n```\nOK\n```", "`print` ต้องเป็นตัวพิมพ์เล็กทั้งหมด"),
    ("จำวงเล็บปิด", "ฝึกเขียนคำสั่งที่ปิดวงเล็บครบ แล้วแสดงคำว่า Closed\n\n**Output:**\n```\nClosed\n```", "ทุก `(` ต้องมี `)`"),
    ("จำเครื่องหมายคำพูด", "แสดงคำว่า Quotes โดยใส่ในเครื่องหมายคำพูดให้ครบคู่\n\n**Output:**\n```\nQuotes\n```", "เปิด `\"` แล้วต้องปิด `\"`"),
    ("จำชื่อ Error: Syntax", "เมื่อวงเล็บ/คำพูดไม่ครบ มักได้ Error ชนิดใด? ให้พิมพ์ชื่อนั้นออกมา\n\n**Output:**\n```\nSyntaxError\n```", "สะกดให้ตรงกับที่ Python แจ้ง"),
    ("จำชื่อ Error: Name", "เมื่อพิมพ์ชื่อคำสั่งผิด เช่น `Print` มักได้ Error ชนิดใด?\n\n**Output:**\n```\nNameError\n```", "ตัวพิมพ์ใหญ่-เล็กสำคัญ"),
    ("อ่านบรรทัดใน Error", "สมมติ Error บอกว่าพังที่บรรทัดแรก — แสดงข้อความเตือนตัวเอง\n\n**Output:**\n```\nline 1\n```", "แค่ print ข้อความตามโจทย์"),
    ("สองคำสั่งติดกัน", "แสดงคำว่า One และ Two คนละบรรทัด เพื่อฝึกรันหลายคำสั่ง\n\n**Output:**\n```\nOne\nTwo\n```", "สอง `print()`"),
    ("ห้ามใช้ Print ตัวใหญ่", "แสดงข้อความเตือนว่าต้องใช้ print ตัวเล็ก\n\n**Output:**\n```\nsmall print\n```", "อย่าเขียน `Print`"),
    ("อ่าน Error ก่อนตกใจ", "แสดงประโยคเตือนใจเวลาเจอ Error\n\n**Output:**\n```\nRead the error message\n```", "พิมพ์ตาม Output ให้ครบช่องว่าง"),
    ("พร้อมเรียน", "แสดงข้อความว่าเซิร์ฟเวอร์การเรียนรู้พร้อมแล้ว\n\n**Output:**\n```\nServer of learning: ready\n```", "ดูเครื่องหมาย `:` และช่องว่าง"),
]

PROMPTS["003-python-syntax"] = [
    ("เช็ก syntax พื้นฐาน", "เขียนคำสั่ง print ที่ถูกกฎ แล้วแสดงว่า syntax โอเค\n\n**Output:**\n```\nsyntax ok\n```", "ตัวพิมพ์เล็ก + วงเล็บ + คำพูดครบ"),
    ("คู่คำพูด", "ฝึกใส่ข้อความในเครื่องหมายคำพูดให้ครบคู่\n\n**Output:**\n```\npair\n```", "`\"pair\"`"),
    ("คู่วงเล็บ", "ฝึกปิดวงเล็บของ print ให้ครบ\n\n**Output:**\n```\nparens\n```", "`print(...)`"),
    ("อย่าเยื้องมั่ว", "เขียนโค้ดชิดซ้ายสุด (อย่าเยื้อง space แปลกๆ) แล้วแสดงข้อความนี้\n\n**Output:**\n```\nno random indent\n```", "บรรทัดแรกสุดของไฟล์ ไม่มี space นำ"),
    ("สองบรรทัดถูกกฎ", "แสดง A แล้ว B คนละบรรทัด ด้วย syntax ที่ถูกต้อง\n\n**Output:**\n```\nA\nB\n```", "สอง print"),
    ("เลขก็เป็นข้อความได้", "แสดงเลข 42 ในรูปข้อความ\n\n**Output:**\n```\n42\n```", 'ใช้ `"42"` ใน print'),
    ("ช่องว่างในประโยค", "แสดงประโยคที่มีช่องว่างคั่นคำ\n\n**Output:**\n```\nHello World\n```", "ช่องว่างอยู่ภายในเครื่องหมายคำพูด"),
    ("เครื่องหมายท้ายประโยค", "แสดงคำตอบสั้นๆ พร้อมเครื่องหมายตกใจ\n\n**Output:**\n```\nYes!\n```", "ใส่ `!` ใน string"),
    ("นับหนึ่งถึงสาม", "แสดงคำว่า one, two, three คนละบรรทัด\n\n**Output:**\n```\none\ntwo\nthree\n```", "สาม print"),
    ("ป้ายจบแบบฝึก", "เมื่อโค้ดผ่านทุกกฎแล้ว ให้แสดงป้ายยืนยันตามนี้\n\n**Output:**\n```\nsyntax done\n```", "พิมพ์ข้อความตาม Output ให้ตรงทุกตัวอักษร"),
]

PROMPTS["004-comments"] = [
    ("comment แล้วทักทาย", "ใส่ comment อธิบายว่ากำลังทักทาย แล้วแสดง `Hi`\n\n**Output:**\n```\nHi\n```\n\n(comment จะไม่โชว์ใน Output)", "ขึ้นต้น comment ด้วย `#`"),
    ("สองขั้นตอน", "เขียน comment สองบรรทัด (step 1 / step 2) แล้วแสดง `OK`\n\n**Output:**\n```\nOK\n```", "comment กี่บรรทัดก็ได้ ขอให้มี `#`"),
    ("comment ท้ายบรรทัด", "แสดง `End` และใส่ comment ท้ายบรรทัดเดียวกันว่าจบโปรแกรม\n\n**Output:**\n```\nEnd\n```", "เขียนได้แบบ `print(...)  # คำอธิบาย`"),
    ("ป้ายชื่อร้าน", "ใส่ comment ว่าเป็นชื่อร้าน แล้วแสดง `Bakery`\n\n**Output:**\n```\nBakery\n```", "comment อธิบาย 'ทำไม' ไม่ใช่แค่พูดซ้ำโค้ด"),
    ("ป้ายราคา", "ใส่ comment เรื่องราคา แล้วแสดงข้อความราคา\n\n**Output:**\n```\nPrice: 50\n```", "ดูช่องว่างหลัง `:`"),
    ("comment ยาวแต่ไม่โชว์", "ใส่ comment ภาษาไทยยาวๆ ได้ตามใจ แล้วแสดงแค่ `Run`\n\n**Output:**\n```\nRun\n```", "คนอ่านเห็น comment — โปรแกรมข้าม"),
    ("หัวข้อหน้า", "ใส่ comment หัวข้อหน้า แล้วแสดง `Title`\n\n**Output:**\n```\nTitle\n```", "อย่างน้อย 1 comment"),
    ("คั่นด้วย comment", "แสดง `A` แล้วมี comment คั่น แล้วแสดง `B`\n\n**Output:**\n```\nA\nB\n```", "comment อยู่ระหว่างสอง print"),
    ("เตือนตัวเอง", "ใส่ comment เตือนเรื่องวงเล็บ แล้วแสดง `Safe`\n\n**Output:**\n```\nSafe\n```", "comment เป็นข้อความอะไรก็ได้ที่เกี่ยวกับการระวัง syntax"),
    ("ปิดสมุดโน้ต", "ใส่ comment ว่าจบแบบฝึก แล้วแสดงข้อความยืนยัน\n\n**Output:**\n```\ncomment practice done\n```", "comment ไม่โชว์ — มีแค่ข้อความจาก print"),
]

PROMPTS["005-variables"] = [
    ("เก็บชื่อแล้วแสดง", "สร้างตัวแปร `name` เก็บค่า `\"Ann\"` แล้วแสดงค่าในตัวแปรนั้น\n\n**Output:**\n```\nAnn\n```", "สร้างตัวแปรก่อน แล้ว `print(name)`"),
    ("เก็บอายุ", "สร้าง `age = 12` แล้วแสดงอายุออกทางหน้าจอ\n\n**Output:**\n```\n12\n```", "ตัวเลขไม่ต้องใส่เครื่องหมายคำพูด"),
    ("ผลรวมของสองกล่อง", "มีกล่อง `x = 5` และ `y = 2` ให้แสดงผลรวมของทั้งสอง\n\n**Output:**\n```\n7\n```", "ใช้ `x + y` ใน print หรือเก็บในตัวแปรก่อนก็ได้"),
    ("เปลี่ยนค่าแล้วค่อยโชว์", "เริ่ม `n = 1` จากนั้นเปลี่ยนเป็น `2` แล้วแสดงค่าสุดท้าย\n\n**Output:**\n```\n2\n```", "ตัวแปรชื่อเดิมใส่ค่าใหม่ทับได้"),
    ("ชื่อเมือง", "สร้าง `city = \"Bangkok\"` แล้วแสดงชื่อเมือง\n\n**Output:**\n```\nBangkok\n```", "ข้อความต้องอยู่ใน `\"...\"`"),
    ("ป้ายราคา", "สร้าง `price = 99` แล้วแสดงราคา\n\n**Output:**\n```\n99\n```", "print ค่าจากตัวแปร"),
    ("สองค่าคนละบรรทัด", "สร้าง `a = 1`, `b = 2` แล้วแสดงทีละค่าคนละบรรทัด\n\n**Output:**\n```\n1\n2\n```", "ใช้ print สองครั้ง"),
    ("รวมสามจำนวน", "กำหนด `p=10`, `q=20`, `r=30` แล้วแสดงผลรวมทั้งสาม\n\n**Output:**\n```\n60\n```", "`p + q + r`"),
    ("ลดเลือดในเกม", "เริ่ม `lives = 3` แล้วลดลง 1 ชีวิต (`lives = lives - 1`) จากนั้นแสดงจำนวนที่เหลือ\n\n**Output:**\n```\n2\n```", "อัปเดตค่าในตัวแปรเดิม"),
    ("คำนวณยอดรวม", "สร้างตัวแปร `total` ให้มีค่าเท่ากับ `40 + 60` แล้วแสดงยอดรวม\n\n**Output:**\n```\n100\n```", "เก็บผลลัพธ์ไว้ในชื่อที่สื่อความหมาย"),
]

PROMPTS["006-naming-rules"] = [
    ("ชื่อแบบ snake_case", "สร้างตัวแปร `first_name` เก็บ `\"Ada\"` แล้วแสดงค่า\n\n**Output:**\n```\nAda\n```", "คั่นคำด้วย `_` ไม่ใช้ช่องว่าง"),
    ("เลิกใช้ชื่อ x", "เก็บคะแนนไว้ในตัวแปรชื่อ `score` ค่า 10 แล้วแสดง\n\n**Output:**\n```\n10\n```", "ตั้งชื่อให้คนอ่านรู้ทันทีว่าคือคะแนน"),
    ("มีตัวเลขต่อท้ายได้", "สร้าง `player1 = 1` แล้วแสดงค่า (ขึ้นต้นด้วยตัวเลขไม่ได้ แต่ต่อท้ายได้)\n\n**Output:**\n```\n1\n```", "ห้ามเขียน `1player`"),
    ("ราคารวม", "สร้าง `total_price = 500` แล้วแสดง\n\n**Output:**\n```\n500\n```", "snake_case สองคำ"),
    ("ค่าความจริง", "สร้าง `is_ok = True` แล้วแสดงค่า\n\n**Output:**\n```\nTrue\n```", "`T` ต้องตัวใหญ่"),
    ("อายุผู้ใช้", "สร้าง `user_age = 15` แล้วแสดง\n\n**Output:**\n```\n15\n```", "ชื่อสื่อว่าเป็นอายุของผู้ใช้"),
    ("นับชิ้นของ", "สร้าง `item_count = 4` แล้วแสดงจำนวนชิ้น\n\n**Output:**\n```\n4\n```", "หลีกเลี่ยงชื่อสั้นเกินอย่าง `c`"),
    ("อย่าชน keyword", "เก็บข้อความ `\"hi\"` ในตัวแปรชื่อ `message` (อย่าไปตั้งชื่อว่า `print`) แล้วแสดง\n\n**Output:**\n```\nhi\n```", "ห้ามใช้คำสงวนเป็นชื่อตัวแปร"),
    ("คะแนนสูงสุด", "สร้าง `max_score = 100` แล้วแสดง\n\n**Output:**\n```\n100\n```", "ชื่อบอกว่าเป็นค่าสูงสุด"),
    ("เช็กกฎการตั้งชื่อ", "แสดงข้อความยืนยันว่าตั้งชื่อแบบ Python style ได้แล้ว\n\n**Output:**\n```\nnaming ok\n```", "print ข้อความตามโจทย์"),
]

PROMPTS["007-data-types"] = [
    ("ชนิดจำนวนเต็ม", "สร้าง `x = 7` แล้วแสดงชนิดข้อมูลด้วย `type(x)`\n\n**Output (ประมาณ):**\n```\n<class 'int'>\n```", "ใช้ `print(type(x))`"),
    ("ชนิดทศนิยม", "สร้าง `y = 1.5` แล้วแสดงชนิดของมัน\n\n**Output (ประมาณ):**\n```\n<class 'float'>\n```", "`type(y)`"),
    ("ชนิดข้อความ", "สร้าง `z = \"hi\"` แล้วแสดงชนิด\n\n**Output (ประมาณ):**\n```\n<class 'str'>\n```", "อย่าลืมเครื่องหมายคำพูด"),
    ("ชนิดจริง/เท็จ", "สร้าง `flag = True` แล้วแสดงชนิด\n\n**Output (ประมาณ):**\n```\n<class 'bool'>\n```", "`True` ตัว T ใหญ่"),
    ("สามชนิดในไฟล์เดียว", "สร้าง `a=1`, `b=1.0`, `c=\"1\"` แล้วแสดงค่าทั้งสาม คนละบรรทัด\n\n**Output:**\n```\n1\n1.0\n1\n```", "สังเกตว่าหน้าตาคล้ายกัน แต่ชนิดต่างกัน"),
    ("ค่า False", "สร้าง `ok = False` แล้วแสดงค่า\n\n**Output:**\n```\nFalse\n```", "`F` ตัวใหญ่"),
    ("เลขในคำพูดคือ str", "แสดงชนิดของ `\"99\"` (มีเครื่องหมายคำพูด)\n\n**Output (ประมาณ):**\n```\n<class 'str'>\n```", "`type(\"99\")`"),
    ("ทศนิยมเล็กๆ", "แสดงชนิดของ `0.25`\n\n**Output (ประมาณ):**\n```\n<class 'float'>\n```", "มีจุดทศนิยม = float"),
    ("บวกจำนวนจริง", "แสดงผลลัพธ์ของ `4 + 5`\n\n**Output:**\n```\n9\n```", "int บวก int ได้เลข"),
    ("ต่อข้อความเลข", "แสดงผลของ `\"4\" + \"5\"` (ต่อข้อความ ไม่ใช่บวกเลข)\n\n**Output:**\n```\n45\n```", "เครื่องหมายคำพูดทำให้เป็น str"),
]

PROMPTS["008-type-conversion"] = [
    ("แปลงข้อความเป็น int", "แปลง `\"8\"` ให้เป็นจำนวนเต็ม แล้วแสดงค่า\n\n**Output:**\n```\n8\n```", "ใช้ `int(\"8\")`"),
    ("แปลงเป็น float", "แปลง `\"2.5\"` เป็นทศนิยม แล้วแสดง\n\n**Output:**\n```\n2.5\n```", "`float(\"2.5\")`"),
    ("ต่อข้อความกับตัวเลข", "ต้องแปลงเลข 12 เป็นข้อความก่อน แล้วต่อกับ `\"Hi:\"`\n\n**Output:**\n```\nHi:12\n```", "ใช้ `str(12)`"),
    ("อายุปีหน้าจากข้อความ", "มี `a = \"5\"` แปลงเป็น int แล้วบวก 1 แสดงผล\n\n**Output:**\n```\n6\n```", "`int(a) + 1`"),
    ("สองเท่า", "แปลง `\"9\"` เป็น int แล้วคูณ 2\n\n**Output:**\n```\n18\n```", "`int(\"9\") * 2`"),
    ("ราคาเป็น float", "แปลง `\"50\"` เป็น float แล้วแสดง\n\n**Output:**\n```\n50.0\n```", "`float(...)`"),
    ("ประกอบข้อความคะแนน", "มี `score = 90` สร้างข้อความ `Score:` ต่อด้วยคะแนน\n\n**Output:**\n```\nScore:90\n```", "`\"Score:\" + str(score)`"),
    ("ปีถัดไป", "แปลง `\"2026\"` เป็น int แล้วบวก 1\n\n**Output:**\n```\n2027\n```", "แปลงก่อนคำนวณ"),
    ("ครึ่งหนึ่ง", "แปลง `\"10\"` เป็น int แล้วหาร 2\n\n**Output:**\n```\n5.0\n```", "ใน Python 3 การหาร `/` ได้ float"),
    ("ชนิดหลังแปลง", "แสดงชนิดของค่าที่ได้จาก `int(\"3\")`\n\n**Output (ประมาณ):**\n```\n<class 'int'>\n```", "`type(int(\"3\"))`"),
]

PROMPTS["009-input"] = [
    ("ทักทายจากชื่อที่พิมพ์", "รับชื่อจากผู้ใช้ แล้วแสดงคำว่า `Hello,` ตามด้วยชื่อ\n\n**ตัวอย่าง** ถ้าพิมพ์ `Mina`\n```\nHello, Mina\n```", "`name = input()` แล้ว `print(\"Hello,\", name)`"),
    ("รับจำนวนแล้วโชว์", "รับจำนวนเต็มหนึ่งตัว แล้วแสดงค่านั้นกลับออกไป\n\n**ตัวอย่าง** Input `7` → Output `7`", "`n = int(input())`"),
    ("อายุปีหน้า", "รับอายุเป็นจำนวนเต็ม แล้วแสดงอายุในปีหน้า (บวก 1)\n\n**ตัวอย่าง** Input `12` → Output `13`", "แปลงเป็น int ก่อนบวก"),
    ("ชื่อจริง-นามสกุล", "รับสองบรรทัด: ชื่อจริง แล้ว นามสกุล จากนั้นแสดงทั้งสองในบรรทัดเดียวคั่นด้วยช่องว่าง\n\n**ตัวอย่าง**\nInput:\n```\nAda\nLovelace\n```\nOutput:\n```\nAda Lovelace\n```", "เรียก `input()` สองครั้ง"),
    ("รับทศนิยม", "รับตัวเลขทศนิยมหนึ่งค่า แล้วแสดงค่านั้น\n\n**ตัวอย่าง** Input `2.5` → Output `2.5`", "`float(input())`"),
    ("คูณสอง", "รับจำนวนเต็ม แล้วแสดงค่าคูณสอง\n\n**ตัวอย่าง** Input `6` → Output `12`", "`n * 2`"),
    ("ผลต่าง", "รับจำนวนเต็มสองตัว (a แล้ว b) แสดงผล `a - b`\n\n**ตัวอย่าง**\nInput:\n```\n10\n3\n```\nOutput:\n```\n7\n```", "สองครั้ง `int(input())`"),
    ("พิมพ์ซ้ำสองรอบ", "รับคำหนึ่งคำ แล้วแสดงคำนั้นสองบรรทัดติดกัน\n\n**ตัวอย่าง** Input `ping` →\n```\nping\nping\n```", "เก็บในตัวแปร แล้ว print สองครั้ง"),
    ("ผลรวมสองจำนวน", "รับจำนวนเต็มสองตัว แล้วแสดงผลรวม\n\n**ตัวอย่าง**\nInput:\n```\n4\n5\n```\nOutput:\n```\n9\n```", "`a + b`"),
    ("เมืองที่อยู่", "รับชื่อเมือง แล้วแสดงในรูปแบบมีป้ายกำกับ\n\n**ตัวอย่าง** Input `Chiang Mai` → Output:\n```\nCity: Chiang Mai\n```", 'ใช้ `print("City:", city)`'),
]

PROMPTS["010-output-formatting"] = [
    ("f-string ทักทาย", "กำหนด `name = \"Bob\"` ใช้ f-string แสดงคำทักทาย\n\n**Output:**\n```\nHi Bob\n```", '`print(f"Hi {name}")`'),
    ("แสดงคะแนนด้วย f-string", "กำหนด `score = 88` แสดงในรูปแบบมีป้ายกำกับ\n\n**Output:**\n```\nScore: 88\n```", "ใส่ตัวแปรใน `{}`"),
    ("ทศนิยม 1 ตำแหน่ง", "กำหนด `x = 3.14159` แสดงเหลือทศนิยม 1 ตำแหน่ง\n\n**Output:**\n```\n3.1\n```", "ใช้ `:.1f`"),
    ("บังคับทศนิยม 2 ตำแหน่ง", "กำหนด `y = 2` แสดงเป็นเงินสองตำแหน่ง\n\n**Output:**\n```\n2.00\n```", "`{y:.2f}`"),
    ("สองค่าในบรรทัดเดียว", "กำหนด `a=1`, `b=2` แสดงคั่นด้วยจุลภาคไม่มีช่องว่าง\n\n**Output:**\n```\n1,2\n```", '`f"{a},{b}"`'),
    ("คั่นด้วยขีด", "แสดงเลข 1 2 3 โดยคั่นด้วย `-` ในบรรทัดเดียว\n\n**Output:**\n```\n1-2-3\n```", "ใช้พารามิเตอร์ `sep=\"-\"`"),
    ("ไม่ขึ้นบรรทัดระหว่างคำ", "พิมพ์ `Hi` โดยไม่ขึ้นบรรทัดใหม่ แล้วพิมพ์ `there` ต่อท้าย\n\n**Output:**\n```\nHi there\n```", '`print("Hi", end=" ")`'),
    ("ป้ายราคาทศนิยม", "กำหนด `price = 99.9` แสดงราคาทศนิยม 2 ตำแหน่ง\n\n**Output:**\n```\nPrice: 99.90\n```", "`:.2f` ใน f-string"),
    ("คำนวณใน f-string", "แสดงผลลัพธ์ของ `3 * 4` โดยเขียนใน f-string\n\n**Output:**\n```\n12\n```", '`f"{3 * 4}"`'),
    ("ประโยคอายุ", "กำหนด `age = 10` แสดงประโยคสั้นๆ\n\n**Output:**\n```\nI am 10\n```", '`f"I am {age}"`'),
]

PROMPTS["011-operators"] = [
    ("เครื่องคิดเลขบวก", "รับจำนวนเต็มสองตัว แล้วแสดงผลบวก\n\n**ตัวอย่าง**\nInput:\n```\n3\n4\n```\nOutput:\n```\n7\n```", "`+`"),
    ("เครื่องคิดเลขคูณ", "รับจำนวนเต็มสองตัว แล้วแสดงผลคูณ\n\n**ตัวอย่าง** Input `5` แล้ว `6` → `30`", "`*`"),
    ("หารแบบจำนวนเต็ม", "แสดงผลของ `17 // 5` (ปัดเศษทิ้ง)\n\n**Output:**\n```\n3\n```", "`//` ไม่ใช่ `/`"),
    ("หาเศษ", "แสดงผลของ `17 % 5`\n\n**Output:**\n```\n2\n```", "`%` คือเศษจากการหาร"),
    ("ยกกำลัง", "แสดงผลของ `2 ** 4`\n\n**Output:**\n```\n16\n```", "`**`"),
    ("เท่ากับสิบไหม", "รับจำนวนเต็ม แล้วแสดงผลเปรียบเทียบว่าเท่ากับ 10 หรือไม่ (ได้ `True`/`False`)\n\n**ตัวอย่าง** Input `10` → `True`", "`n == 10`"),
    ("ผ่านเกณฑ์ 50 ไหม", "ครูตั้งเกณฑ์ผ่านที่ 50 คะแนน\nรับคะแนนของนักเรียน แล้วแสดงผลเปรียบเทียบ `score >= 50` เป็นค่า True/False\n\n**ตัวอย่าง**\n| Input | Output |\n|-------|--------|\n| `50` | `True` |\n| `49` | `False` |", "พิมพ์ผลของนิพจน์เปรียบเทียบตรงๆ ไม่ต้อง if"),
    ("ลำดับการคำนวณ", "แสดงผลของนิพจน์ `2 + 3 * 4` (คูณก่อน)\n\n**Output:**\n```\n14\n```", "อย่าคิดว่าบวกก่อน"),
    ("ใช้วงเล็บบังคับลำดับ", "แสดงผลของ `(2 + 3) * 4`\n\n**Output:**\n```\n20\n```", "วงเล็บทำให้บวกก่อน"),
    ("หารธรรมดา", "แสดงผลของ `9 / 2`\n\n**Output:**\n```\n4.5\n```", "`/` ได้ทศนิยม"),
]

PROMPTS["012-review"] = [
    ("วันทบทวน", "แสดงข้อความบอกว่าวันนี้เป็นวันทบทวน\n\n**Output:**\n```\nReview day\n```", "print"),
    ("ทบทวนตัวแปร", "สร้าง `a = 5` แล้วแสดงค่า\n\n**Output:**\n```\n5\n```", "variable + print"),
    ("ทบทวนแปลงชนิด", "แปลง `\"7\"` เป็น int แล้วบวก 3\n\n**Output:**\n```\n10\n```", "`int(\"7\") + 3`"),
    ("ทบทวน input", "รับข้อความหนึ่งบรรทัด แล้วแสดงข้อความนั้นกลับไป\n\n**ตัวอย่าง** Input `hello` → Output `hello`", "`print(input())` ก็ได้"),
    ("ทบทวน f-string", "กำหนด `n = 2` แสดงในรูปแบบ `n=2`\n\n**Output:**\n```\nn=2\n```", "f-string"),
    ("ทบทวนบวกจาก input", "รับจำนวนเต็มสองตัว แสดงผลรวม\n\n**ตัวอย่าง**\nInput:\n```\n8\n2\n```\nOutput:\n```\n10\n```", "int(input) สองครั้ง"),
    ("ทบทวนทศนิยม", "แสดงค่า `(2 / 3)` ให้เหลือทศนิยม 1 ตำแหน่ง\n\n**Output:**\n```\n0.7\n```", "`:.1f`"),
    ("ทบทวน modulo", "แสดงผลของ `10 % 4`\n\n**Output:**\n```\n2\n```", "%"),
    ("ทบทวน type", "แสดงชนิดของค่า `True`\n\n**Output (ประมาณ):**\n```\n<class 'bool'>\n```", "`type(True)`"),
    ("ทบทวนรวมทักษะ", "รับจำนวนเต็ม แล้วแสดงในรูปแบบมีป้าย `Value:`\n\n**ตัวอย่าง** Input `15` →\n```\nValue: 15\n```", "int + f-string"),
]

PROMPTS["013-if-else"] = [
    ("จำนวนบวกหรือไม่", "รับจำนวนเต็ม ถ้ามากกว่า 0 แสดง `Positive` ไม่เช่นนั้นแสดง `Non-positive`\n\n**ตัวอย่าง** Input `5` → `Positive` / Input `0` → `Non-positive`", "`if n > 0:` ... `else:`"),
    ("เป็นศูนย์ไหม", "รับจำนวนเต็ม ถ้าเท่ากับ 0 แสดง `Zero` ไม่เช่นนั้น `Other`\n\n**ตัวอย่าง** Input `0` → `Zero`", "`==`"),
    ("บัตรผู้ใหญ่", "รับอายุ ถ้า `>= 18` แสดง `Adult` ไม่เช่นนั้น `Kid`\n\n**ตัวอย่าง** Input `17` → `Kid`", "`>=`"),
    ("รหัสผ่านตัวเลข", "รับรหัสเป็นจำนวนเต็ม ถ้าเท่ากับ `1234` แสดง `OK` ไม่เช่นนั้น `No`\n\n**ตัวอย่าง** Input `1234` → `OK`", "เปรียบเทียบด้วย `==`"),
    ("อากาศร้อนไหม", "รับอุณหภูมิ (°C) ถ้า `>= 30` แสดง `Hot` ไม่เช่นนั้น `Cool`\n\n**ตัวอย่าง** Input `31` → `Hot`", "if/else"),
    ("ได้ส่วนลดไหม", "รับยอดซื้อ ถ้า `>= 100` แสดง `Discount` ไม่เช่นนั้น `Normal`\n\n**ตัวอย่าง** Input `99` → `Normal`", "เกณฑ์ที่ยอดซื้อ"),
    ("คำยาวไหม (จำลองด้วยตัวเลข)", "รับตัวเลขแทนความยาวคำ ถ้า `> 5` แสดง `Long` ไม่เช่นนั้น `Short`\n\n**ตัวอย่าง** Input `6` → `Long`", "`>`"),
    ("เลขคู่หรือคี่", "รับจำนวนเต็ม ใช้ `% 2` ตัดสิน แสดง `Even` หรือ `Odd`\n\n**ตัวอย่าง** Input `8` → `Even`", "`n % 2 == 0`"),
    ("ชมเชยคะแนน", "รับคะแนน ถ้า `>= 80` แสดง `Great` ไม่เช่นนั้น `Keep going`\n\n**ตัวอย่าง** Input `80` → `Great`", "if/else"),
    ("ติดลบไหม", "รับจำนวนเต็ม ถ้า `< 0` แสดง `Neg` ไม่เช่นนั้น `Non-neg`\n\n**ตัวอย่าง** Input `-3` → `Neg`", "`<`"),
]

PROMPTS["014-even-odd"] = [
    ("ตรวจเลขคู่-คี่", "รับจำนวนเต็ม แสดง `Even` ถ้าหาร 2 ลงตัว ไม่เช่นนั้น `Odd`\n\n**ตัวอย่าง** Input `7` → `Odd`", "`% 2`"),
    ("แสดงเศษจากการหาร 2", "รับจำนวนเต็ม แล้วแสดงค่า `n % 2` (ได้ 0 หรือ 1)\n\n**ตัวอย่าง** Input `9` → `1`", "ไม่ต้อง if ก็ได้"),
    ("หารสามลงตัวไหม", "รับจำนวนเต็ม ถ้า `% 3 == 0` แสดง `Yes` ไม่เช่นนั้น `No`\n\n**ตัวอย่าง** Input `9` → `Yes`", "% 3"),
    ("เลขกลมหลักสิบ", "รับจำนวนเต็ม ถ้าหาร 10 ลงตัว แสดง `Round` ไม่เช่นนั้น `Not`\n\n**ตัวอย่าง** Input `40` → `Round`", "`% 10 == 0`"),
    ("หลักหน่วย", "รับจำนวนเต็ม แสดงเฉพาะหลักหน่วย (`n % 10`)\n\n**ตัวอย่าง** Input `127` → `7`", "% 10"),
    ("คู่หรือคี่อีกรอบ", "รับจำนวนเต็ม แสดง `Even`/`Odd` ตามเศษจากการหาร 2\n\n**ตัวอย่าง** Input `2` → `Even`", "ทบทวนแบบเดิมในสถานการณ์ใหม่: นับคะแนนเกม"),
    ("ศูนย์ถือว่าคู่", "รับจำนวนเต็ม (ลองใส่ 0) แล้วตัดสิน Even/Odd ตามกฎ `%`\n\n**ตัวอย่าง** Input `0` → `Even`", "0 หาร 2 เหลือ 0"),
    ("ป้ายทะเบียนจำลอง", "รับเลขท้ายป้าย ถ้าเป็นคี่แสดง `Odd plate` ถ้าคู่แสดง `Even plate`\n\n**ตัวอย่าง** Input `5` → `Odd plate`", "`% 2 != 0`"),
    ("จัดบล็อกที่นั่ง", "รับเลขที่นั่ง ถ้าหาร 4 ลงตัวไป `Block A` ไม่เช่นนั้น `Block B`\n\n**ตัวอย่าง** Input `8` → `Block A`", "% 4"),
    ("ตรวจแล้วปิดท้าย", "รับจำนวนเต็ม แสดง Even/Odd แล้วพิมพ์บรรทัดตามว่า `Checked`\n\n**ตัวอย่าง** Input `4` →\n```\nEven\nChecked\n```", "มี print หลัง if/else"),
]

PROMPTS["015-elif"] = [
    ("เครื่องหมายจำนวน", "รับจำนวนเต็ม: มากกว่า 0 → `Pos`, น้อยกว่า 0 → `Neg`, เท่า 0 → `Zero`\n\n**ตัวอย่าง** Input `-1` → `Neg`", "if / elif / else"),
    ("ไซส์เสื้อจากตัวเลข", "รับขนาดตัวเลข: `>=100` → `L`, `>=50` → `M`, อื่นๆ → `S`\n\n**ตัวอย่าง** Input `75` → `M`", "เรียงเงื่อนไขจากมากไปน้อย"),
    ("ระดับอากาศ", "สถานีอากาศจัดระดับอุณหภูมิเป็น 3 ชั้น:\n\n| เงื่อนไข | แสดง |\n|----------|------|\n| `t >= 35` | `Hot` |\n| `t >= 20` (แต่ยังไม่ถึง 35) | `Warm` |\n| นอกนั้น | `Cold` |\n\n**ตัวอย่าง** Input `22` → `Warm` / Input `10` → `Cold`", "เรียง if → elif → else จากเงื่อนไขเข้มไปหลวม"),
    ("เลเวลผู้เล่น", "รับเลเวล: `>=10` High, `>=5` Mid, อื่นๆ Low\n\n**ตัวอย่าง** Input `5` → `Mid`", "`>=` กับ elif"),
    ("เส้นผ่านเกณฑ์", "รับคะแนน: `>50` Pass, `==50` Border, อื่นๆ Fail\n\n**ตัวอย่าง** Input `50` → `Border`", "ระวังลำดับเงื่อนไข"),
    ("มื้ออาหารตามเวลา", "รับชั่วโมง (0–23): `<11` Breakfast, `<15` Lunch, อื่นๆ Dinner\n\n**ตัวอย่าง** Input `12` → `Lunch`", "ตรวจจากบนลงล่าง"),
    ("ความเร็วรถ", "รับความเร็ว: `>=120` Fast, `>=60` OK, อื่นๆ Slow\n\n**ตัวอย่าง** Input `60` → `OK`", "elif"),
    ("เกรดละเอียด", "รับคะแนน: `>=90` S, `>=80` A, `>=70` B, อื่นๆ C\n\n**ตัวอย่าง** Input `85` → `A`", "หลาย elif"),
    ("เลือกเครื่องดื่ม", "รับเมนูเลข: `1` Water, `2` Juice, อื่นๆ Other\n\n**ตัวอย่าง** Input `2` → `Juice`", "`==` กับ elif"),
    ("ผลการเรียนภาค", "รับคะแนน: `>=60` Pass, `>=40` Retake, อื่นๆ Fail\n\n**ตัวอย่าง** Input `45` → `Retake`", "3 ทางด้วย elif"),
]

PROMPTS["016-logical-operator"] = [
    ("ทั้งคู่เป็นบวก", "รับจำนวนสองตัว ถ้าทั้งคู่ `> 0` แสดง `Both` ไม่เช่นนั้น `No`\n\n**ตัวอย่าง**\nInput:\n```\n2\n5\n```\nOutput: `Both`", "ใช้ `and`"),
    ("มีศูนย์ปนไหม", "รับจำนวนสองตัว ถ้ามีอย่างน้อยหนึ่งตัวเป็น 0 แสดง `Has zero` ไม่เช่นนั้น `None`\n\n**ตัวอย่าง** Input `0` แล้ว `8` → `Has zero`", "ใช้ `or`"),
    ("ไม่ฝนตกก็ออกไป", "รับค่า raining เป็น `1` หรือ `0` — ถ้าไม่ฝน (`not`) แสดง `Go out` ไม่เช่นนั้น `Stay`\n\n**ตัวอย่าง** Input `0` → `Go out`", "`if not r:`"),
    ("อยู่ในช่วง 1–10", "รับจำนวนเต็ม ถ้าอยู่ระหว่าง 1 ถึง 10 inclusive แสดง `In range` ไม่เช่นนั้น `Out`\n\n**ตัวอย่าง** Input `10` → `In range`", "`n >= 1 and n <= 10`"),
    ("ผ่านวิชา", "รับคะแนนสอบ และเปอร์เซ็นต์เข้าเรียน — ผ่านเมื่อคะแนน `>=50` และเข้าเรียน `>=80`\n\nแสดง `Pass` หรือ `Fail`\n\n**ตัวอย่าง**\nInput:\n```\n55\n90\n```\n→ `Pass`", "and"),
    ("ได้ส่วนลดสมาชิก", "รับสถานะสมาชิก (`1`/`0`) และยอดซื้อ — ได้ส่วนลดถ้าเป็นสมาชิกหรือยอด `>=1000`\n\nแสดง `Discount` หรือ `No`", "or"),
    ("ล็อกอินง่าย", "รับ username ถูกไหม (`1`/`0`) และ password ถูกไหม (`1`/`0`) — ทั้งคู่ถูกถึงจะ `Login` ไม่งั้น `Deny`", "and"),
    ("สูงหรืออายุไม่ถึง", "รับอายุและส่วนสูง ถ้าอายุ `<13` หรือสูง `<100` แสดง `Too small` ไม่เช่นนั้น `OK`", "or"),
    ("กลางวันหรือกลางคืน", "รับค่า night เป็น `1`/`0` — ถ้าไม่ใช่ night แสดง `Day` ไม่เช่นนั้น `Night`", "not"),
    ("ดาวเด่น", "รับคะแนน, โบนัส (`1`/`0`), เหรียญวิเศษ (`1`/`0`) — ได้ `Star` เมื่อคะแนน `>=80` และ (โบนัสหรือเหรียญเป็น 1)\n\nไม่งั้น `Normal`", "and รวมกับ or ในวงเล็บ"),
]

PROMPTS["017-for-loop"] = [
    ("ทักทายสามรอบ", "ใช้ `for` กับ `range(3)` พิมพ์คำว่า `Hi` สามครั้ง\n\n**Output:**\n```\nHi\nHi\nHi\n```", "`for i in range(3):`"),
    ("นับจากศูนย์", "พิมพ์ค่า `i` จาก `range(4)` คือ 0 ถึง 3\n\n**Output:**\n```\n0\n1\n2\n3\n```", "range ไม่รวมตัวจบ"),
    ("นับหนึ่งถึงห้า", "พิมพ์เลข 1 ถึง 5 ด้วย `range(1, 6)`\n\n**Output:**\n```\n1\n2\n3\n4\n5\n```", "start, stop"),
    ("เลขคู่ด้วย step", "พิมพ์เลขคู่ 2 ถึง 10 ด้วย `range(2, 11, 2)`\n\n**Output:**\n```\n2\n4\n6\n8\n10\n```", "พารามิเตอร์ step"),
    ("ดาวห้าแถว", "วาดดาวเป็นเสาสูง 5 ชั้น — แต่ละชั้นมีดาว 1 ดวง คนละบรรทัด\n\n**Output:**\n```\n*\n*\n*\n*\n*\n```", "ใช้ `for` กับ `range(5)` แล้ว `print(\"*\")` ในลูป"),
    ("รับจำนวนรอบ", "รับจำนวนเต็ม `n` แล้วพิมพ์ `OK` จำนวน n ครั้ง\n\n**ตัวอย่าง** Input `2` →\n```\nOK\nOK\n```", "`range(n)`"),
    ("สะสมในลูป", "เริ่ม `total = 0` แล้วในลูป 3 รอบให้บวก 1 เข้า total สุดท้ายแสดง total\n\n**Output:**\n```\n3\n```", "อัปเดตตัวแปรในลูป"),
    ("นับถอยหลัง", "พิมพ์ 3, 2, 1 ด้วย `range(3, 0, -1)`\n\n**Output:**\n```\n3\n2\n1\n```", "step ติดลบ"),
    ("ตารางห้า", "พิมพ์ 5, 10, 15, 20, 25 ด้วย range ที่ step = 5\n\n**Output:**\n```\n5\n10\n15\n20\n25\n```", "`range(5, 26, 5)`"),
    ("กำลังสอง", "สำหรับ i = 1 ถึง 4 พิมพ์ค่า `i * i`\n\n**Output:**\n```\n1\n4\n9\n16\n```", "`range(1, 5)`"),
]

PROMPTS["018-loop-with-lists"] = [
    ("เดินทีละตัวอักษร", "มีลิสต์ `[\"a\", \"b\", \"c\"]` ใช้ for แสดงทีละตัว\n\n**Output:**\n```\na\nb\nc\n```", "`for x in ...`"),
    ("นับสมาชิกด้วย len", "มีกล่องของเล่น `nums = [1, 2, 3, 4]`\nให้แสดงว่ามีของกี่ชิ้น (ใช้ `len`)\n\n**Output:**\n```\n4\n```", "`print(len(nums))`"),
    ("ตัวแรกของลิสต์", "แสดงสมาชิกตำแหน่งแรกของ `[9, 8, 7]`\n\n**Output:**\n```\n9\n```", "index เริ่มที่ 0"),
    ("รวมเลขในลิสต์", "รวมค่าใน `[1, 2, 3]` ด้วย for แล้วแสดงผลรวม\n\n**Output:**\n```\n6\n```", "ใช้ตัวสะสม total"),
    ("คูณสองแล้วโชว์", "สำหรับแต่ละค่าใน `[2, 4]` แสดงค่าคูณสอง\n\n**Output:**\n```\n4\n8\n```", "วนแล้วคำนวณ"),
    ("ความยาวลิสต์ซ้ำ", "แสดง `len([1, 1, 1])`\n\n**Output:**\n```\n3\n```", "len นับชิ้น ไม่สนค่าซ้ำ"),
    ("สีสองสี", "วนลิสต์ `[\"red\", \"blue\"]` แสดงชื่อสีทีละบรรทัด\n\n**Output:**\n```\nred\nblue\n```", "for กับ list ของ str"),
    ("นับที่มากกว่าห้า", "ใน `[3, 6, 9]` นับว่ามีกี่ตัวที่ `> 5` แล้วแสดงจำนวน\n\n**Output:**\n```\n2\n```", "ใช้ if ในลูปได้ (เรียนแล้ว)"),
    ("แสดงสองจำนวน", "วน `[10, 20]` แสดงทีละค่า\n\n**Output:**\n```\n10\n20\n```", "for"),
    ("ค่าเฉลี่ยง่าย", "จากลิสต์ `[10, 20]` หาผลรวมด้วย for แล้วหารด้วยจำนวนสมาชิก แสดงค่าเฉลี่ย\n\n**Output:**\n```\n15.0\n```", "`total / len(xs)`"),
]

PROMPTS["019-while-loop"] = [
    ("นับหนึ่งถึงสามด้วย while", "ใช้ while พิมพ์ 1, 2, 3\n\n**Output:**\n```\n1\n2\n3\n```", "อย่าลืม `i = i + 1`"),
    ("ตะโกน Go ห้าครั้ง", "ใช้ while พิมพ์ `Go` ห้าครั้ง\n\n**Output:** บรรทัด `Go` × 5", "ตัวนับรอบ"),
    ("นับจากศูนย์", "พิมพ์ 0, 1, 2 ด้วย while\n\n**Output:**\n```\n0\n1\n2\n```", "`i <= 2`"),
    ("ผลรวมหนึ่งถึงสี่", "ใช้ while หาผลรวม 1+2+3+4 แล้วแสดง\n\n**Output:**\n```\n10\n```", "สะสมใน total"),
    ("นับเลือดลง", "เริ่ม lives=3 พิมพ์ค่า lives ทีละรอบแล้วลด 1 จนหมด\n\n**Output:**\n```\n3\n2\n1\n```", "เงื่อนไข `lives > 0`"),
    ("อ่านจนเจอศูนย์", "รับจำนวนซ้ำ แสดงทุกจำนวนที่อ่านได้ จนกระทั่งอ่านได้ 0 (ไม่ต้องพิมพ์ 0)\n\n**ตัวอย่าง**\nInput:\n```\n4\n5\n0\n```\nOutput:\n```\n4\n5\n```", "`while n != 0`"),
    ("ทวีคูณจนเกินแปด", "เริ่ม `x = 1` ขณะที่ `x <= 8` ให้พิมพ์ x แล้วคูณสอง\n\n**Output:**\n```\n1\n2\n4\n8\n```", "`x = x * 2`"),
    ("พิมพ์ A สามครั้ง", "ใช้ while พิมพ์ `A` สามครั้ง", "ตัวนับ 0..2"),
    ("นับถึงห้า", "พิมพ์ 0 ถึง 5 ด้วย while\n\n**Output:** สุดท้ายเป็น 5", "`i <= 5`"),
    ("ทำครั้งเดียวอย่างปลอดภัย", "ใช้ while กับธง `done` พิมพ์ `Once` เพียงครั้งเดียวแล้วหยุด\n\n**Output:**\n```\nOnce\n```", "ตั้ง `done = True` ในลูป"),
]

PROMPTS["020-loop-safety"] = [
    ("หยุดก่อนถึงสาม", "วน `range(10)` แต่ `break` เมื่อ `i == 3` (พิมพ์แค่ 0 1 2)\n\n**Output:**\n```\n0\n1\n2\n```", "break ก่อนหรือหลัง print ให้ตรงโจทย์นี้: พิมพ์ก่อนเช็กหรือเช็กก่อนพิมพ์ — ตามเฉลยคือเช็กก่อนแล้วค่อยพิมพ์ ดังนั้นไม่พิมพ์ 3"),
    ("ข้ามเลขสอง", "วน `range(5)` ใช้ `continue` ข้ามเมื่อ `i == 2`\n\n**Output:**\n```\n0\n1\n3\n4\n```", "continue"),
    ("while True แล้วเลิก", "เข้า `while True` พิมพ์ `Start` แล้ว `break` ทันที\n\n**Output:**\n```\nStart\n```", "ต้องมี break"),
    ("พิมพ์เฉพาะคี่", "วน 1 ถึง 5 ข้ามเลขคู่ด้วย continue\n\n**Output:**\n```\n1\n3\n5\n```", "`if i % 2 == 0: continue`"),
    ("หาเก้าแล้วหยุด", "ในลิสต์ `[1, 2, 9, 4]` พิมพ์ทีละตัว และเมื่อเจอ 9 ให้ break (พิมพ์ 9 ด้วย)\n\n**Output:**\n```\n1\n2\n9\n```", "print ก่อน break"),
    ("รวมจนเจอศูนย์", "รับจำนวนซ้ำ รวมเฉพาะค่าที่ไม่ใช่ 0 เมื่อเจอ 0 ให้หยุดแล้วแสดงผลรวม\n\n**ตัวอย่าง**\nInput:\n```\n5\n5\n0\n```\nOutput:\n```\n10\n```", "while True + break"),
    ("ข้ามศูนย์ในลิสต์", "จาก `[1, 0, 2]` พิมพ์เฉพาะค่าที่ไม่ใช่ 0\n\n**Output:**\n```\n1\n2\n```", "continue เมื่อเป็น 0"),
    ("while ที่อัปเดตครบ", "พิมพ์ 1 2 3 ด้วย while อย่างปลอดภัย (มีอัปเดตตัวแปร)\n\n**Output:**\n```\n1\n2\n3\n```", "กัน infinite loop"),
    ("กรองช่วงกลาง", "วน `range(10)` ข้ามค่า `< 3` และหยุดเมื่อ `> 7` — พิมพ์ค่าที่เหลือ\n\n**Output:**\n```\n3\n4\n5\n6\n7\n```", "continue + break"),
    ("จบหลัง break", "ทำ `while True: break` แล้วค่อยพิมพ์ `Done`\n\n**Output:**\n```\nDone\n```", "โค้ดหลังลูปยังทำงาน"),
]

PROMPTS["021-nested-loops"] = [
    ("ตารางดาว 2×2", "ใช้ลูปซ้อนพิมพ์ดาวเป็นตาราง 2 แถว แถวละ 2 ดาว\n\n**Output:**\n```\n**\n**\n```", '`print("*", end="")` แล้ว `print()` จบแถว'),
    ("พิมพ์คู่พิกัด", "นอก `range(2)` ใน `range(2)` แสดงค่า i และ j\n\n**Output:**\n```\n0 0\n0 1\n1 0\n1 1\n```", "print(i, j)"),
    ("สามเหลี่ยมดาว", "แถวที่ 1 ถึง 3 ให้พิมพ์ดาวตามหมายเลขแถว\n\n**Output:**\n```\n*\n**\n***\n```", "`for k in range(row)`"),
    ("Hi ซ้อนกัน", "ลูปนอก 2 รอบ ลูปใน 2 รอบ พิมพ์ `Hi` รวม 4 ครั้ง", "นอก×ใน = จำนวนรอบ"),
    ("ตารางคูณมุมเล็ก", "สำหรับ i,j ใน 1..2 พิมพ์ค่า `i*j` คนละบรรทัด\n\n**Output:**\n```\n1\n2\n2\n4\n```", "nested range(1,3)"),
    ("แถวตัวเลข 1 2 3", "สองแถว แต่ละแถวพิมพ์ `1 2 3 ` (มีช่องว่างท้ายตาม end)\n\nสังเกตช่องว่างจาก `end=\" \"`", "ลูปในพิมพ์ j"),
    ("กากบาทหกรอบ", "นอก 3 รอบ ใน 2 รอบ พิมพ์ `x` รวม 6 บรรทัด", "3×2"),
    ("ตัวเลขเรียงตามแถว", "แถว i=1..3 พิมพ์เลข 1..i ติดกันไม่มีช่องว่าง แล้วขึ้นบรรทัดใหม่\n\n**Output:**\n```\n1\n12\n123\n```", "`end=\"\"`"),
    ("a เว้นวรรคแล้ว b", "สองรอบ พิมพ์ `a` (ไม่ขึ้นบรรทัด) แล้ว `b`\n\n**Output:**\n```\na b\na b\n```", "end=\" \""),
    ("สี่เหลี่ยมตามขนาด", "รับ n แล้วพิมพ์สี่เหลี่ยม `n x n` ด้วยตัวอักษร `#`\n\n**ตัวอย่าง** Input `2` →\n```\n##\n##\n```", "nested ตาม n"),
]

PROMPTS["022-loop-review"] = [
    ("ทบทวน for", "พิมพ์ 0 1 2 ด้วย for-range\n\n**Output:**\n```\n0\n1\n2\n```", "range(3)"),
    ("ทบทวน while", "พิมพ์ 1 และ 2 ด้วย while\n\n**Output:**\n```\n1\n2\n```", "อัปเดตตัวแปร"),
    ("ทบทวนวนลิสต์", "วน `[5, 6]` แสดงทีละค่า\n\n**Output:**\n```\n5\n6\n```", "for item in list"),
    ("ทบทวน break", "วน range(5) พิมพ์ค่า และหยุดหลังพิมพ์ 2\n\n**Output:**\n```\n0\n1\n2\n```", "print แล้วค่อย break"),
    ("ทบทวน continue", "วน range(4) ข้ามเมื่อ i==1\n\n**Output:**\n```\n0\n2\n3\n```", "continue"),
    ("ทบทวนผลรวม", "รวมค่าใน range(1,4) แสดงผลรวม\n\n**Output:**\n```\n6\n```", "1+2+3"),
    ("ทบทวนซ้อน", "พิมพ์ตาราง `#` ขนาด 2×2\n\n**Output:**\n```\n##\n##\n```", "nested + end"),
    ("ทบทวน while True", "พิมพ์ `X` หนึ่งครั้งด้วย while True แล้ว break\n\n**Output:**\n```\nX\n```", "break"),
    ("ทบทวนนับคู่", "นับเลขคู่ใน `[1,2,3,4]` แสดงจำนวน\n\n**Output:**\n```\n2\n```", "% 2 == 0"),
    ("ป้ายผ่านทบทวนลูป", "ถ้าทำครบแบบฝึกทบทวนแล้ว ให้ติดป้ายนี้บนจอ\n\n**Output:**\n```\nloops ok\n```", "พิมพ์ข้อความตาม Output ให้ตรง"),
]

PROMPTS["023-debugging-loops"] = [
    ("แก้ range ให้ครบ 1..3", "ต้องการพิมพ์ 1 ถึง 3 — ใช้ `range` ให้ถูกต้อง\n\n**Output:**\n```\n1\n2\n3\n```", "`range(1, 4)` ไม่ใช่ `range(1, 3)`"),
    ("อย่าลืมอัปเดต while", "พิมพ์ `Hi` สองครั้งด้วย while โดยมีตัวนับเพิ่มทุกครั้ง\n\n**Output:**\n```\nHi\nHi\n```", "ลืมอัปเดต = วนไม่รู้จบ"),
    ("เยื้องให้ถูก", "ใช้ for พิมพ์ 0 1 2 โดยเยื้องบรรทัดในลูปให้ถูก\n\n**Output:**\n```\n0\n1\n2\n```", "indent ใน for"),
    ("พิมพ์สมาชิก ไม่ใช่ทั้งลิสต์", "วน `[\"A\", \"B\"]` ให้พิมพ์ทีละชื่อ\n\n**Output:**\n```\nA\nB\n```", "print ตัวแปรวน ไม่ใช่ชื่อลิสต์"),
    ("ใส่ debug ชั่วคราว", "วน range(2) แสดงค่า i แบบ `i=0` / `i=1`\n\n**Output:**\n```\ni=0\ni=1\n```", "f-string ช่วยไล่บั๊ก"),
    ("เลือก range ให้พอดี", "ต้องการพิมพ์ 0 ถึง 3 ใช้ range ที่ถูกต้อง\n\n**Output:**\n```\n0\n1\n2\n3\n```", "`range(4)`"),
    ("เงื่อนไข while ให้ตรง", "พิมพ์ 0 1 2 ด้วย while ที่เงื่อนไข `i < 3`\n\n**Output:**\n```\n0\n1\n2\n```", "เริ่ม i=0"),
    ("while True แบบมีทางออก", "พิมพ์ `End` แล้ว break\n\n**Output:**\n```\nEnd\n```", "ต้องมี break"),
    ("ข้ามศูนย์ด้วย continue", "วน range(3) ข้ามเมื่อ i==0 เหลือ 1 และ 2\n\n**Output:**\n```\n1\n2\n```", "continue"),
    ("บั๊กหายแล้ว", "หลังไล่บั๊กครบ ให้แสดงข้อความฉลองเล็กๆ\n\n**Output:**\n```\nbugs fixed\n```", "พิมพ์ตาม Output"),
]

PROMPTS["024-midyear-review"] = [
    ("ทักทายจากชื่อ", "โปรแกรมต้อนรับแขก: รับชื่อจากแป้นพิมพ์ แล้วทักทายในรูปแบบ `Hello,` ตามด้วยชื่อ\n\n**ตัวอย่าง** Input `Lee` → `Hello, Lee`", "`print(\"Hello,\", name)` มีช่องว่างอัตโนมัติจาก print"),
    ("บวกห้าจาก input", "เครื่องคิดเลขมินิ: รับจำนวนเต็มหนึ่งค่า แล้วแสดงผลหลังบวกเพิ่มอีก 5\n\n**ตัวอย่าง** Input `10` → `15`", "แปลงด้วย `int(input())` ก่อนบวก"),
    ("ผ่าน-ไม่ผ่าน", "ตัดเกรดผ่าน/ตก ด้วยเกณฑ์ 50\n\n| คะแนน | แสดง |\n|--------|------|\n| `>= 50` | `Pass` |\n| น้อยกว่า 50 | `Fail` |\n\n**ตัวอย่าง** Input `50` → `Pass`", "if / else"),
    ("คู่หรือคี่", "ตัวแยกเลขคู่-คี่: รับจำนวนเต็ม แล้วแสดง `Even` หรือ `Odd`\n\n**ตัวอย่าง** Input `3` → `Odd` / Input `4` → `Even`", "ใช้ `% 2 == 0`"),
    ("เกรดคร่าวๆ", "ตัดเกรดแบบหยาบ 3 ระดับ:\n\n| คะแนน | เกรด |\n|--------|------|\n| `>= 80` | `A` |\n| `>= 60` | `B` |\n| นอกนั้น | `C` |\n\n**ตัวอย่าง** Input `75` → `B`", "เรียง if → elif → else"),
    ("นับหนึ่งถึง n", "รับจำนวนเต็ม `n` แล้วพิมพ์เลขตั้งแต่ 1 ถึง n คนละบรรทัด (เหมือนนับของ)\n\n**ตัวอย่าง** Input `3` →\n```\n1\n2\n3\n```", "`for i in range(1, n + 1)`"),
    ("รวมลิสต์สั้น", "มีคะแนนสามครั้ง `[2, 2, 2]` ให้รวมด้วย `for` แล้วแสดงผลรวม\n\n**Output:**\n```\n6\n```", "เริ่มตัวสะสมที่ 0 แล้วบวกทีละตัว"),
    ("นับด้วย while", "ฝึก while: พิมพ์ 1, 2, 3 คนละบรรทัด โดยใช้นับแล้วเพิ่มค่าเอง\n\n**Output:**\n```\n1\n2\n3\n```", "อย่าลืมอัปเดตตัวแปรทุกครั้งในลูป"),
    ("เฉลี่ยสวยๆ", "คำนวณค่าเฉลี่ยของคะแนน 10 กับ 20 แล้วแสดงทศนิยม 1 ตำแหน่ง\n\n**Output:**\n```\n15.0\n```", "ใช้ f-string กับ `:.1f`"),
    ("ป้ายปิด midyear", "ติดป้ายยืนยันว่าผ่านแบบฝึกครึ่งปีแล้ว\n\n**Output:**\n```\nmidyear done\n```", "พิมพ์ข้อความตาม Output ให้ตรงทุกตัวอักษร"),
]

TOPICS = {
    "001-what-is-python": "print()",
    "002-python-environment": "Environment",
    "003-python-syntax": "Syntax",
    "004-comments": "Comments",
    "005-variables": "Variables",
    "006-naming-rules": "Naming Rules",
    "007-data-types": "Data Types",
    "008-type-conversion": "Type Conversion",
    "009-input": "Input",
    "010-output-formatting": "Output Formatting",
    "011-operators": "Operators",
    "012-review": "Review",
    "013-if-else": "if/else",
    "014-even-odd": "Even/Odd",
    "015-elif": "elif",
    "016-logical-operator": "Logical Operators",
    "017-for-loop": "for Loop",
    "018-loop-with-lists": "Loop with Lists",
    "019-while-loop": "while Loop",
    "020-loop-safety": "Loop Safety",
    "021-nested-loops": "Nested Loops",
    "022-loop-review": "Loop Review",
    "023-debugging-loops": "Debugging Loops",
    "024-midyear-review": "Midyear Review",
}


def main() -> None:
    total = 0
    for week, items in PROMPTS.items():
        assert len(items) == 10, week
        topic = TOPICS[week]
        week_dir = HW / week
        for i, (title, body, hint) in enumerate(items, start=1):
            ans = (week_dir / "answer" / f"{i:02d}.py").read_text(encoding="utf-8")
            starter = fix_mod.scaffold(ans)
            if starter.strip() == ans.strip():
                starter = "# เขียนโค้ดตรงนี้\n"
            path = week_dir / f"{i:02d}.md"
            path.write_text(md(topic, i, title, body, hint, starter), encoding="utf-8")
            total += 1
    # Fix broken example in 009-01 if any weird space
    print(f"rewrote {total} homework files across {len(PROMPTS)} weeks")


if __name__ == "__main__":
    main()
