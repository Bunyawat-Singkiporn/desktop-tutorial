# 🐛 Debugging — Question 1: Fix a Syntax Error

**Difficulty:** 🟢 Easy

---

## Problem

The code below has a **syntax error**. Find and fix it so the code can run.

```python
score = 75

if score >= 50
    print("Pass")
else
    print("Fail")
```

**Correct Output:**
```
Pass
```

---

## 💡 Hint

Python requires a `:` after every `if` and `else`.

---

## Starter Code

```python
score = 75

# Fix the syntax errors below
if score >= 50
    print("Pass")
else
    print("Fail")
```
