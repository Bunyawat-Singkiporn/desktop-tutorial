# 🔢 Practice Return Values — Question 2: Is Even

**Difficulty:** 🟢 Easy

---

## Problem

Create an `is_even(n)` function that returns `True` for an even number or `False` for an odd number.
Then use the function in an `if`/`else` statement.

**Output:**
```
4 → Even
7 → Odd
10 → Even
```

---

## Starter Code

```python
def is_even(n):
    # Write your code here

for num in [4, 7, 10]:
    if is_even(num):
        print(f"{num} → Even")
    else:
        print(f"{num} → Odd")
```
