# 🧠 กฎการเขียน Python — ข้อ 13: ปริศนาเครื่องหมายคำพูด

**Difficulty:** 🔴 Challenge

---

## โจทย์

เขียนโปรแกรมแสดงข้อความ 3 บรรทัดนี้ ซึ่งมีเครื่องหมายคำพูดปนอยู่ข้างใน

```text
He said "hello" to me
It's a sunny day
She wrote 'python' on the board
```

> บรรทัดที่ 1 มี `"` · บรรทัดที่ 2 มี `'` · บรรทัดที่ 3 มี `'`
> ต้องเลือกเครื่องหมายคำพูดข้างนอกให้เหมาะกับแต่ละบรรทัด

---

## Input

ไม่มี

## Output

ข้อความ 3 บรรทัด

---

## ตัวอย่าง

**Output:**

```text
He said "hello" to me
It's a sunny day
She wrote 'python' on the board
```

---

## 💡 Hint

กฎง่ายๆ — ข้างในมี `"` ให้ใช้ `'` ครอบ · ข้างในมี `'` ให้ใช้ `"` ครอบ
ห้ามใช้ชนิดเดียวกันทั้งข้างนอกและข้างใน

---

## Starter Code

```python
# เขียนโค้ดตรงนี้
```
