# ⬅️ กฎการเขียน Python — ข้อ 4: ชิดซ้ายให้หมด

**Difficulty:** 🟢 Easy

---

## โจทย์

โค้ดนี้ผิดกฎข้อที่ 3 — มีบรรทัดที่เว้นวรรคนำหน้าโดยไม่จำเป็น

```python
print("Bus Schedule")
   print("07:00 to school")
      print("16:30 to home")
print("Fare 10 baht")
```

เขียนใหม่ให้ถูกกฎ

---

## Input

ไม่มี

## Output

ข้อความ 4 บรรทัด

---

## ตัวอย่าง

**Output:**

```text
Bus Schedule
07:00 to school
16:30 to home
Fare 10 baht
```

---

## 💡 Hint

ช่องว่างหน้าบรรทัดจะมีความหมายพิเศษตอนเรียน `if` กับ `for` ตอนนี้ยังไม่ถึงเวลา ให้ชิดซ้ายให้หมด

---

## Starter Code

```python
# เขียนโค้ดตรงนี้
```
